<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.diki.myprofile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/interview"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splashScreenPager.view_pager"/>
        <activity android:name=".login.Login" />
        <activity android:name=".main.HomeActivity" />
        <activity android:name=".sign_up.ActivitySignUp" />
        <activity android:name=".friend_detail.FriendDetailActivity" />
        <activity
            android:name=".main.MainActivity"
            android:label="10116352-DIKI S" />
        <activity
            android:name=".main.SplashScreen"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".list_friends.AddFriend" />
    </application>

</manifest>